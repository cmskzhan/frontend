#select panel use panel number
set -g display-panes-time 5000 
bind-key / display-panes \; select-pane -t :. 


# Use Alt-vim keys without prefix key to switch panes
bind-key -n M-Left select-pane -L 
bind-key -n M-Right select-pane -R 
bind-key -n M-Up select-pane -U 
bind-key -n M-Down select-pane -D 

#bind-key K swap-pane -U
#bind-key J swap-pane -D

#change copy mode shortcutw
bind -n S-Pageup copy-mode -u

# use mouse, copy paste need shift key now
set -g mouse on

#change pane foreground, background color
#select-pane -t:.1 -P 'fg=blue,bg=red'